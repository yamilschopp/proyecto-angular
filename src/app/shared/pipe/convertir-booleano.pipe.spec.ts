import { ConvertirBooleanoPipe } from './convertir-booleano.pipe';

describe('ConvertirBooleanoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirBooleanoPipe();
    expect(pipe).toBeTruthy();
  });
});
